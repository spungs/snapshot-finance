import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/snapshot-finance',
  assetPrefix: '/snapshot-finance/',
};

export default nextConfig;
